<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Gallery" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.Gallery.Starting" parent="Theme.SplashScreen.IconBackground">
        <!--        <item name="windowSplashScreenBackground">@color/design_default_color_background</item>-->
        <item name="windowSplashScreenAnimatedIcon">@drawable/launcher_icon_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/launcher_icon_v3_background
        </item>
        <!-- Required for animated icons. -->
        <!--        <item name="windowSplashScreenAnimationDuration">200</item>-->
        <item name="postSplashScreenTheme">@style/Theme.Gallery</item>
    </style>

</resources>
